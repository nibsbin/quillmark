[alias]
# The `test` job's doc gate, minus `--locked`. A hand-listed subset drifts from
# what CI documents, which is the whole workspace.
doc-workspace = "doc --no-deps --workspace"

[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']