{
  "name": "quillmark-wasm-tests",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Vitest integration tests for quillmark-wasm",
  "scripts": {
    "test": "bash ../../../scripts/build-wasm.sh && vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite-plugin-wasm": "^3.5.0",
    "vitest": "^4.1.10"
  }
}
